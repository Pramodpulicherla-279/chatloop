services:
  - type: web
    name: chatloop-server
    env: node
    buildCommand: npm install && npm run build
    startCommand: npm start
    healthCheckPath: /
    autoDeploy: true
    envVars:
      - key: PORT
        value: 10000
